query GetOrderByUserId($userId: String!) {
  getOrderByUserId(userId: $userId) {
    id
    orderNumber
    orderDate
    status
    quantity
    subtotal
    product {
      images
      name
      price
    }
  }
}
